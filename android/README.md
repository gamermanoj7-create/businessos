# BusinessOS Android

Android client foundation for the ERP phases. It is designed around a local Room database + remote NestJS API so offline-first sync can be expanded without changing the business domain.

Build with Android Studio using the Gradle wrapper generated by Android Studio, then set the API base URL in the Retrofit provider before production deployment.
