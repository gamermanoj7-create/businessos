import {Body,Controller,Get,Param,Patch,Post,UseGuards} from '@nestjs/common'; import {ContactsService} from './contacts.service'; import {CurrentBusiness} from '../common/decorators/current-business.decorator'; import {Permissions} from '../common/decorators/permissions.decorator'; import {PERMISSIONS} from '../common/constants/permissions.constant'; import {TenantGuard} from '../common/guards/tenant.guard'; import {PermissionsGuard} from '../common/guards/permissions.guard';
@UseGuards(TenantGuard,PermissionsGuard) @Controller() export class ContactsController{constructor(private readonly s:ContactsService){}
@Get('customers')@Permissions(PERMISSIONS.CUSTOMER_VIEW)customers(@CurrentBusiness()t:any){return this.s.listCustomers(t.businessId)} @Post('customers')@Permissions(PERMISSIONS.CUSTOMER_CREATE)createCustomer(@CurrentBusiness()t:any,@Body()d:any){return this.s.createCustomer(t.businessId,d)} @Patch('customers/:id')@Permissions(PERMISSIONS.CUSTOMER_UPDATE)updateCustomer(@CurrentBusiness()t:any,@Param('id')id:string,@Body()d:any){return this.s.updateCustomer(t.businessId,id,d)}
@Get('suppliers')@Permissions(PERMISSIONS.SUPPLIER_VIEW)suppliers(@CurrentBusiness()t:any){return this.s.listSuppliers(t.businessId)} @Post('suppliers')@Permissions(PERMISSIONS.SUPPLIER_CREATE)createSupplier(@CurrentBusiness()t:any,@Body()d:any){return this.s.createSupplier(t.businessId,d)} @Patch('suppliers/:id')@Permissions(PERMISSIONS.SUPPLIER_UPDATE)updateSupplier(@CurrentBusiness()t:any,@Param('id')id:string,@Body()d:any){return this.s.updateSupplier(t.businessId,id,d)}
}
